tags:
  - Auth
summary: Log in a user
operationId: loginUser
description: Authenticate user
requestBody:
  required: true
  content:
    application/json:
      schema:
        type: object
        required:
          - email
          - password
        properties:
          email:
            type: string
            example: valya@gmail.com
          password:
            type: string
            format: password
            example: GoIt2025
responses:
  '200':
    description: User logged in
    content:
      application/json:
        schema:
          type: object
          properties:
            status:
              type: integer
              example: 200
            message:
              type: string
              example: Successfully logged in!
            data:
              type: object
              properties:
                user:
                  type: string
                  example: 65e4decdd286b30065d54af9
                accessToken:
                  type: string
  '404':
    $ref: '../../components/responses/404.yaml'
